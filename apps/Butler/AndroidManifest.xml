<?xml version="1.0" encoding="utf-8"?>
<!--
  Copyright (C) 2024 CircleOS
  SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="za.co.circleos.butler">

    <uses-permission android:name="com.circleos.permission.ACCESS_INFERENCE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_butler"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".MeshActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="Mesh Chat" />

        <!-- Receives incoming mesh MSG_TEXT frames broadcast by CircleMeshService -->
        <receiver
            android:name=".MeshMessageReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="za.co.circleos.mesh.action.MESSAGE_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
