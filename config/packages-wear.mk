# Circle OS - Watch app group. core apps (incl. Panik SOS + Messages) + a round
# watch home + a tiny curated set. Everything else folds away.
PRODUCT_PACKAGES += \
    CircleStartWatch \
    CircleGlance \
    CircleReminders \
    CircleFindMy
